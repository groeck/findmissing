findmissing - find missing commits in stable releases

To initialize, run "sh setup.sh" once.

Subsequently, run "python initdb.py", which will update the databases.
Run "python missing.py" to get a list of commits missing from stable
releases.
